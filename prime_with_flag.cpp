#include <bits/stdc++.h>
using namespace std;
int main() {
   int num , flag = 0;
   cin>>num;
   for(int i=2 ; i < num/2 ; i++) {
      if(num%i == 0) {
         
         if(flag = 1);
         {
             cout<<num<<"is not prime";
             break;
         }
         
      
   else {
      cout<<num<<"is prime";
   }
      }
}
  //another method
  #include <bits/stdc++.h>
using namespace std;
int main() {
   int num , flag = 0;
   cin>>num;
   for(int i=2 ; i < num ; i++) {
      if(num%i == 0) {
         
         if(flag = 1);
         {
             cout<<num<<"is not prime";
             break;
         }
         
      
   else {
      cout<<num<<"is prime";
   }
      }
}
  // 3rd method
  #include <bits/stdc++.h>
using namespace std;
int main() {
   int num , flag = 0;
   cin>>num;
   for(int i=2 ; i < sqrt(num) ; i++) {
      if(num%i == 0) {
         
         if(flag = 1);
         {
             cout<<num<<"is not prime";
             break;
         }
         
      
   else {
      cout<<num<<"is prime";
   }
      }
}
